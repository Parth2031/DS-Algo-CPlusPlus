// ! Incomplete Questions ->
// * All Index Of Numbers (checking array)
// * Sum Of Digits in String (1 case) [?]
// * Pallindrome of strings (1 case)
// * Move Chararcters (1 case)               { Didn't Understood }

// TODO: Remaining Questions (Unattempted) -->    
// ? Binomial Pattern
// ? Number To Strings(Recursion).   
// ? Move Characters (Recursion).
// ? Separate Duplicates (Recursion).


#include<iostream>
#include<vector>
using namespace std;

string SumOfDigitsInString(string &n,int stridx)
{
   
}

int main(int args,char** argv)
{
  string num;
  cout<<"Enter the number:";
  cin>>num;
  cout<<"Sum: "<<SumOfDigitsInString(num,0);
  return 0;
}








// //--------------------------------------------------------------------------------------------------------------------------------------------

// ! All Index of Number-->

// * size of array(n)
// * arr[n]
// * check-value number(m)
// * An Input-->:> I'm going to use "for" verification that you returned  an array or not.
// * Find All Index of Number Example->> size(9), Input arr{2,8,6,4,8,6,4,9,0}, number m(8), Input_4 (1) and Output(4). 

// ! Binomial Pattern ????

// //--------------------------------------------------------------------------------------------------------------------------------------------

// ! With Return type ?? (Odd & Even num Printing) -->
// ! Reverse & Inverse of Array--> {Reverse & Inverse array using return type} (???)
// ! Selection & Bubble Sort--> {Selection & Bubble Sort using return type} (???)
// * Selection & Bubble Sort {using single variable}  (???)

// //--------------------------------------------------------------------------------------------------------------------------------------------